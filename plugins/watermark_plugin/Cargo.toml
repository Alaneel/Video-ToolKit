[package]
name = "watermark_plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
plugin_system = { path = "../../crates/plugin_system", features = ["dynamic"] }

[features]
dynamic = []